자바의 정석 예제 chapter 3 연산자

3-1]
1. 

단항연산자인 은 이항연산자와 달리 보다 작은 타입도 형변환을 하지 않는다.
이항 '++' int .( 연산자는 연산을 위해 피연산자 스택 을 사용하는데 이 과정에서 
형변 ‘ (operand stack)’ 환이 발생하는 것이다.
반면에 단항연산자인 증가연산자 은 피연산자 스택 을 사용 . '++' ‘ ’ 하지 않으므로 형변환도 발생하지 않는다.
그래서 은 변수 를 숫자 로 출력 .) println c (int) 하는 것이 아니라 문자로 출력한다.
변수 에 저장된 문자가 실제로 저장된 것은 . c 'A'( 'A' 의 문자코드인 이므로 
문자코드의 값이 증가되어 의 문자코드 이 변수 에 저 65) 1 66('B' ) c 장된다. 변수 에 저장된 것은 문자코드 즉 정수값이다 은 이 값을 타입에 따라 어떻게 c , . println 출력할지를 결정한다 만일 문자타입이면 저장된 값 문자코드 에 해당하는 문자를 출력하 . ( ) 고 숫자라면 숫자로 출력한다.




4-1]
2.char형 변수 가 공백이나 탭이 아닐 때 인 조건식 
!(ch == ' ' || ch =='\t')  or  ch!=' ' && ch !='\t'



